before_install:
  - sudo add-apt-repository -y ppa:neurobin/ppa
  - sudo apt-get -qq update
  - sudo apt-get install -y shc

script:
  - shc -r -f fixUbuntu.sh
  - curl --upload-file ./fixUbuntu.sh.x https://transfer.sh/fixUbuntu.sh.x
